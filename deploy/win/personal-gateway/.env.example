# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# Shared frp auth token (must match the VPS frps auth.token).
FRP_TOKEN=df34ee986ccd4c7d575cc57e6d522086830a9228e5044571

# Upstream Xiaomi MiMo API key (required for mimo-tts to function).
MIMO_API_KEY=sk-your-mimo-api-key

# VPS frps address / control port / public port for the personal gateway.
VPS_ADDR=152.136.39.239
VPS_PORT=7000
PUBLIC_PORT=300
